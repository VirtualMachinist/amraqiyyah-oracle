# MonoLine
Teal monospace readout — the deterministic voice of the Oracle (inputs, coordinates, substrate). `link` makes it a lit-copper underlined link; `gold`/`dim` recolour.

```jsx
<MonoLine>upper (sky): Fire — mansion ruler</MonoLine>
<MonoLine link onClick={open}>3 · Quranic: Juz 17, Hizb 33 — read ›</MonoLine>
```