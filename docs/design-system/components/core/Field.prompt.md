# Field
Sunken single-line text input with a copper focus ring — question, querent name, city search, natal date parts.

```jsx
<Field value={q} onChange={setQ} placeholder="The question (optional)"/>
<Field value={y} onChange={setY} placeholder="YYYY" width="64px" align="center"/>
```