# OracleCard
A deck-card slot honouring the ratified placeholder constraints — uniform backs, split faces, sideways Agent placement. Imagery is pending the Deck Design Document.

```jsx
<OracleCard kind="key" label="Platonic Key" sub="Fire · Al-Nur"/>
<OracleCard kind="agent" label="Mars" sub="RESONANCE" sideways/>
<OracleCard kind="bridge" label="Bridge 3" sub="yang face" split/>
```

kinds: `axial` ☶ · `key` ◈ · `agent` ✶ (sideways) · `bridge` (split face).